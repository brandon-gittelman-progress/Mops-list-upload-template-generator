import{setStep,store}from"../store.js";const names=["Upload","Map","Confirm","Settings","Review","Export"];export function renderStepIndicator(){const w=store.get().wizard;return `<div class="step-indicator" role="progressbar" aria-valuemin="1" aria-valuemax="6" aria-valuenow="${w.step}">${names.map((n,i)=>`<button id="step-dot-${i+1}" class="step-dot ${w.step===i+1?'active':w.maxCompletedStep>i+1?'done':''}" ${i+1<=w.maxCompletedStep?'':'disabled'}><span class="circle">${i+1}</span><span>${n}</span>${i===2&&!w.step3Applicable?'<span class="pill">Skipped</span>':''}</button>`).join("")}</div>`}export function bindStepIndicator(){for(let i=1;i<=6;i++)document.getElementById(`step-dot-${i}`)?.addEventListener("click",()=>setStep(i))}