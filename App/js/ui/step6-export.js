import{store}from"../store.js";import{exportCsv,exportXlsx,buildFilename}from"../modules/exporter.js";export function render(){const s=store.get();return `<section><h1 class="step-title">Export Your File</h1><p class="step-subtitle">Choose your format and download the formatted file.</p>${s.exportWithErrors?'<div class="banner">This file contains validation errors. It may fail to import.</div>':''}<div class="export-options"><div class="export-card"><h2>CSV</h2><p>${buildFilename(s,'csv')}</p><button id="download-csv" class="btn btn-primary">Download CSV</button></div><div class="export-card"><h2>XLSX</h2><p>${buildFilename(s,'xlsx')}</p><button id="download-xlsx" class="btn btn-secondary">Download XLSX</button></div></div></section>`}export function bind(){document.getElementById("download-csv")?.addEventListener("click",()=>exportCsv(store.get()));document.getElementById("download-xlsx")?.addEventListener("click",()=>exportXlsx(store.get()))}