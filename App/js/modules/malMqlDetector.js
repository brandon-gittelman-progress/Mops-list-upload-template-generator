export const malMqlDetector={scan(rows){const byRow=new Map();rows.forEach((row,rowIndex)=>{for(const [sourceColumn,value]of Object.entries(row)){if(/^\s*MAL\s*$/i.test(String(value??"")))byRow.set(rowIndex,{rowIndex,kind:"MAL",sourceColumn});else if(/^\s*MQL\s*$/i.test(String(value??""))&&!byRow.has(rowIndex))byRow.set(rowIndex,{rowIndex,kind:"MQL",sourceColumn})}});return [...byRow.values()]},hasDetections(file){return(file.detectedMalMqlRows||[]).length>0},countsByKind(file){return(file.detectedMalMqlRows||[]).reduce((a,d)=>{a[d.kind]=(a[d.kind]||0)+1;return a},{MAL:0,MQL:0})},wouldOverwrite(file,malCol,mqlCol){return(file.rows||[]).filter(r=>(malCol&&r[malCol])||(mqlCol&&r[mqlCol])).length}};